using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerInputs : Player
{
    //put the movement inputs here (and another all type of inputs)

    /*
    private void (string deviceType)
    {
        if(deviceType == "desktop")
        {
            receber inputs WASD e Arrows e para cada input retornar um valor que vai ser enviado para outras 
            classes e ser responsável por diversas operações
        }
        if(deviceType == "mobile")
        {
    
        }
        if(deviceType == "joystick")
        {
    
        }


        if (Input.GetKey(KeyCode.A) || Input.GetKey(KeyCode.LeftArrow))
        {
            if (this.gameObject.transform.position.x > LevelBoundary.leftSide)
            {
                transform.Translate(Vector3.left * Time.deltaTime * leftRightSpeed);
            }
        }

        if (Input.GetKey(KeyCode.D) || Input.GetKey(KeyCode.RightArrow))
        {
            if (this.gameObject.transform.position.x < LevelBoundary.rightSide)
            {
                transform.Translate(Vector3.right * Time.deltaTime * leftRightSpeed);

            }
        }

    }

    */
}
